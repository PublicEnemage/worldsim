# Demo 7 Internal Review — v0.18.0

**Date:** 2026-06-29
**Milestone:** M18
**Demo version:** v0.18.0
**Step:** 6b — Nine-Agent Internal Review Panel
**Gate:** Pre-Step-7 (Independent Review) gate

## Panel composition

| Agent | Lens | Verdict |
|---|---|---|
| UX Designer | Zone layout, ADR-008, ADR-010, UX-RULING-4 | FAIL |
| Data Architect | Schema integrity, CI methodology, tier consistency | FAIL |
| Business PO | Acceptance criteria, north star test, stakeholder value | FAIL |
| Customer Agent (Solo-Use) | Solo-use walk, Layer 3 quality gate | SOLO-USE NOT PASSED |
| Development Economist (DIC) | Human cost ledger, distributional visibility | FAIL |
| QA Lead | Acceptance criteria coverage, testid validation | FAIL |
| Frontend Architect | Rendering fidelity, ADR-008/010/019, component contracts | FAIL |
| UX Design Thinking Agent | Cognitive task alignment, NARRATION-RULING-1, mode fidelity | FAIL |
| Chief Methodologist (DIC) | No False Precision, CI bands, tier badges, direction-stability | FAIL |

**Overall Step 6b gate verdict: FAIL — 9 of 9 agents.**

The demo cannot proceed to Step 7 (Independent Review Agent) without resolving all CRITICAL findings. Screenshots as captured do not support the walkthrough narration for any of the five frames.

---

## Screenshot file evidence

```
docs/demo/m18/screenshots/
  frame-a-the-instrument.png      — 200,024 bytes
  frame-b-uncertainty-envelope.png — 200,024 bytes   ← IDENTICAL to Frame A (byte-for-byte)
  frame-c-act1-finding.png        — 199,576 bytes
  frame-d-counter-proposal.png    — 326,159 bytes
  frame-e-analytical-defence.png  — 326,155 bytes   ← 4-byte diff from Frame D (icon glyph only)
```

MD5 for A and B: identical. 4-byte difference between D and E: consistent with a single toggle arrow glyph change, not a rendered panel.

---

## Findings

### CRITICAL

---

**DEMO-130: Frames A and B are byte-for-byte identical — no distinct "Uncertainty Envelope" capture exists**

Severity: CRITICAL
Confirmed by: All 9 agents
Frames: A, B
What is shown: `frame-b-uncertainty-envelope.png` and `frame-a-the-instrument.png` share an identical MD5 checksum and identical file size (200,024 bytes). Step counter, Zone 1A content, Zone 1B alerts, Zone 1D values, ControlPlaneColumn state — pixel-identical.
What should be shown: Frame B is the designated "uncertainty envelope" frame. Its purpose is to demonstrate CI ribbons as calibrated semi-transparent fills distinguishable from trajectory lines, and to show the `psp-driver-row` as secondary focus. With Frame B being a duplicate of Frame A, the walkthrough Step 2 narration — "Look at Zone 1A. The trajectories now carry uncertainty bands... Now look at Zone 1D — The PSP severity badge reads WARNING. Below it: 'Driver: fiscal sustainability'" — has no corresponding visual state to display.
Root cause: `demo-narrated.spec.ts` captures Frame B at the same UI state as Frame A (both at step 3, same Mode 3 state, same form inputs applied). No distinct `page.screenshot()` state exists between them.
Fix: Capture Frame A at step 8 (Step 5d-confirmed maximum divergence step — see DEMO-135). Frame B at step 3 will then be naturally distinct. Do not attempt to differentiate via annotation alone — the step state difference is the intended distinction. Resolve DEMO-132 (psp-driver-row) and DEMO-137 (CI band geometry) before recapture.

---

**DEMO-131: Zone 3 methodology panel toggle fires but no content in viewport — Frame E is 4 bytes different from Frame D (icon glyph only)**

Severity: CRITICAL
Confirmed by: 8 of 9 agents (all except Development Economist who noted it as DEMO-006 HIGH)
Frames: E
What is shown: Frame E's toggle arrow changes from ▶ to ▼ (confirming the expand click fired) but the BandingEngine note, half-width schedule reference, tier multiplier value (1.5×), and direction-stability condition text are not visible anywhere in Zone 1B. The 4-byte file difference from Frame D is consistent only with the glyph change; no panel content rendered.
What should be shown: Frame E exists specifically to demonstrate that "the methodology behind +342,737 persons is visible and auditable from the primary viewport" (G5 deliverable, #1422). A Persona 1 reviewer (Lucas, analytical economist) must be able to read the BandingEngine CI methodology note from the primary viewport without scrolling. Frame E as captured shows only that the toggle icon changed.
Root cause: Either (a) the expanded panel renders off-screen below Zone 1B's constrained height (CSS `overflow: hidden` clipping), or (b) the panel body content fails to mount on expand (the toggle fires a state change but the content component does not render). Both are supported by the observation that Zone 1B height budget is already constrained by Option A/B/C status rows + TERMINAL alert + collapsed comparison summary header.
Fix: Determine whether the expanded content is rendering off-screen (add `scrollIntoView` before screenshot) or not rendering at all (fix `DistributionalComparisonSummary.tsx` expand handler). At minimum, add `await page.waitForSelector('[data-testid="banding-engine-note"]')` before Frame E screenshot in `demo-narrated.spec.ts`. Architectural fix: ensure the expanded Zone 3 panel fits within Zone 1B viewport at 1440×900 without scroll — resolving DEMO-133 (DistributionalComparisonSummary sticky-bottom) is a prerequisite.

---

**DEMO-132: `psp-driver-row` ("Driver: fiscal sustainability") absent from Zone 1D in all five frames — G2 deliverable not visible anywhere in the demo**

Severity: CRITICAL
Confirmed by: 9 of 9 agents (UX Designer, Data Architect, PO, Customer Agent, Dev Economist, QA Lead CRITICAL, Frontend Architect HIGH, UX DT Agent HIGH, Chief Methodologist CRITICAL)
Frames: A, B, C, D, E (all)
What is shown: Zone 1D Political Risk section shows "Programme survival: WARNING (47%) — STABLE / Comparable: Ghana 2014 ECF — modified Step 5 (PSP 52%). Conditionality relaxed." No "Driver: [category]" label appears in any of the five frames.
What should be shown: G2 sprint deliverable (#1255) — `psp-driver-row` in `FourFrameworkZone1D.tsx` rendering "Driver: fiscal sustainability" beneath the PSP severity badge. The walkthrough Section 2 Step 2 narration is built entirely around this label: "The PSP severity badge reads WARNING. Below it: 'Driver: fiscal sustainability.'" Persona 3 (Andreas) is scripted to cite Programme survival risk "driven by fiscal sustainability." Zero frames show this text.
Root cause: Either (a) `dominant_driver` field in the PSP payload for the SEN scenario is not populated in the demo fixture, or (b) the `psp-driver-row` DOM element exists but renders below the visible Zone 1D boundary at 1440×900.
Fix: Run `data-testid="psp-driver-row"` visibility check in Playwright against the demo environment. If visible in DOM but off-screen, adjust Zone 1D layout. If not visible at all, verify `_attribute_dominant_driver()` returns a non-None value for the SEN fixture at steps 3 and 6, and that `FourFrameworkZone1D.tsx` mounts the driver row element. Recapture all five frames only after `psp-driver-row` confirmed visible at 1440×900.

---

**DEMO-133: DistributionalComparisonSummary full sentence not visible in Frames D/E — CI bounds, "Direction stable", reference label below viewport cutoff**

Severity: CRITICAL
Confirmed by: All 9 agents
Frames: D, E
What is shown: Zone 1B shows the DISTRIBUTIONAL COMPARISON header and "Poverty headcount differential" subheading. "+342,737 persons" is partially visible but truncated at the Zone 1C boundary. CI band bounds ("295K–395K"), the "95% CI" marker, "Direction stable" text, and "vs. Option C" reference scenario label are not visible in either frame.
What should be shown: The walkthrough Act 2 narration — "Three things about that sentence" — depends on all six elements being readable: (a) differential number; (b) CI bounds "295K–395K"; (c) confidence marker "95% CI"; (d) T3 tier badge; (e) "Direction stable" direction stability text; (f) "vs. Option C" reference scenario label. Only the T3 header badge is legible. The "+342,737 persons" visible as a bare number without its CI qualifiers directly instantiates the false precision condition the demo is designed to refute.
Root cause: `DistributionalComparisonSummary` `position: sticky; bottom: 0` is either overridden by a parent container `overflow: hidden`, or the Zone 1B height is insufficient to show the full summary row above the Zone 1C boundary. The truncation of the number at the right edge also indicates a Zone 1B width constraint.
Fix: (1) Correct sticky-bottom CSS — verify parent container uses `overflow: auto` not `overflow: hidden`. (2) Widen Zone 1B or reduce summary component padding to prevent right-edge truncation. (3) Verify all six items are simultaneously legible at 1440×900 before recapture. Use `locator('[data-testid="distributional-comparison-summary"]').screenshot()` for targeted verification.

---

**DEMO-134: Bottom quintile informal workers cohort row absent from Zone 1B in Frame C — wrong cohort group displayed**

Severity: CRITICAL
Confirmed by: 7 of 9 agents (UX Designer, Development Economist CRITICAL, QA Lead HIGH, Frontend Architect CRITICAL, UX DT Agent, Chief Methodologist implicitly, Data Architect)
Frames: C
What is shown: Zone 1B COHORT IMPACT section displays two CRITICAL rows: "Bottom Quintile Agricultural Workers — Poverty Headcount..." and "Bottom Quintile Formal Sector — Poverty Headcount Ratio...". The informal workers cohort is entirely absent.
What should be shown: Frame C is "the Act 1 finding." The screenshot brief requires five items readable: (a) indicator name — "Bottom quintile informal workers poverty headcount"; (b) current value at step 6; (c) recovery floor 0.40; (d) T3 tier badge; (e) threshold state — safe or crossed. The Step 5d gate confirms "MDA outcome: CLEAR in both trajectories (floor 0.40, baseline HD 0.450 at step 6)" against the informal workers floor. None of the five required items are visible because the wrong cohort is shown.
Root cause: Either (a) the SEN fixture does not include the bottom quintile informal workers poverty headcount indicator with threshold=0.40, or (b) the cohort is registered but ordered below the visible cutoff in `CohortImpactSection` (which shows only breaching rows, causing the cleared-threshold informal workers row to disappear when the threshold is not crossed).
Fix: (1) Verify `GET /api/v1/scenarios/{id}/cohort-indicators` response for the SEN fixture includes bottom quintile informal workers. (2) If `CohortImpactSection` hides non-breaching rows, add a "monitored" row state that displays designated focal indicators at every step regardless of breach status. (3) Recapture Frame C only after the informal workers row is confirmed visible at step 6.

---

**DEMO-135: Frame A captured at step 3/8, not step 8 — Step 5d confirmed maximum divergence is at step 8**

Severity: CRITICAL
Confirmed by: QA Lead (CRITICAL), Frontend Architect (MEDIUM), UX Design Thinking Agent (CRITICAL as secondary), Development Economist (noted)
Frames: A
What is shown: Step counter reads "Step 3/8". Zone 1D values at step 3: Financial 0.54, HD 0.46, Governance 0.55. Financial (step 3): Baseline 0.50, Branch 0.54, +0.04.
What should be shown: The Step 5d gate document records "Maximum divergence step: 8 (HD +0.020, Financial +0.040)." The screenshot brief specifies "Senegal at the step identified by the Step 5d panel as showing maximum baseline/branch divergence." Frame A is the thesis frame — it must show the simulation's strongest argument for the counter-proposal. At step 3, the branch has only just been applied; the divergence curve has not reached its peak. Zone 1D values at step 3 rather than step 8 mean the presenter is citing non-peak figures throughout the Act 1 narration.
Fix: Advance to step 8 before the Frame A screenshot action in `demo-narrated.spec.ts`. ControlPlaneColumn state (FM=0.85, branch_from_step=3) is unchanged. Zone 1A already shows the full arc — the step counter advance updates only Zone 1D values and Zone 1B panel state.

---

**DEMO-136: Frame C (Act 1 conclusion) shows branch delta = 0.00 at step 6 — no policy divergence visible at the conclusion step**

Severity: CRITICAL
Confirmed by: UX Design Thinking Agent (CRITICAL), QA Lead (noted as Financial +0.00), multiple agents
Frames: C
What is shown: Frame C Financial header reads "Baseline: 0.49, Branch: 0.49, 0.00 (green)." The FiscalMultiplier=0.85 effect applied at step 3 has fully dissipated by step 6.
What should be shown: Frame C is the Act 1 conclusion. The walkthrough provides two conditional scripts: one where the threshold is not crossed under the tested configuration, one where it is crossed at every configuration. Both require the branch and baseline to show different values at the conclusion step so the presenter can name them and contrast them. A delta of 0.00 makes neither script deliverable. The audience cannot see whether the counter-proposal makes a difference.
Root cause: Step 5d confirmed maximum divergence is at step 8, not step 6. The Act 1 conclusion step is misaligned with the step of maximum branch/baseline divergence.
Fix: Re-capture Frame C at step 8 (consistent with DEMO-135 fix — both Frame A and Frame C should be at the maximum-divergence step). Alternatively, if step 6 has special narrative significance, adjust the branch application step so divergence is visible at step 6. File a sprint issue to reconcile Act 1 conclusion step with Step 5d findings.

---

### HIGH

---

**DEMO-137: CI band geometry bug — fills from trajectory value to chart ceiling rather than ±half-width envelope; y-axis scales to band ceiling not trajectory values**

Severity: HIGH
Confirmed by: QA Lead (HIGH), Frontend Architect (HIGH), UX Design Thinking Agent (HIGH), Chief Methodologist (MEDIUM)
Frames: A, B, C, D, E (all)
What is shown: Zone 1A CI band fill spans approximately Y=0.35 to Y=0.99 in Mode 3 frames (where trajectory values sit near Y=0.50–0.54). The CI band occupies ~80% of chart height; trajectory lines are visible only at the bottom of the band.
What should be shown: Semi-transparent fills as calibrated ±half-width ribbons centered on each trajectory curve. At T3 with Mode 3 opacity 0.05, bands should form narrow ribbons following trajectory curvature. The current geometry appears to fill from the trajectory value upward to the chart ceiling (or from chart floor to trajectory value), not as a symmetric ±envelope.
Secondary effect: Adaptive Y-axis scales to the CI band ceiling (0.99) instead of the trajectory value range, compressing the baseline/branch curves (delta 0.04) to occupy less than 5% of chart height — rendering them visually indistinguishable (DEMO-138 secondary cause).
Fix: Audit `TrajectoryView.tsx` CI band rendering. The fill geometry should be `[mean - halfWidth, mean + halfWidth]` at each step. Verify `CI_BAND_OPACITY_MODE3 = 0.05` constant is applied; rendered opacity appears substantially higher. After fix, verify adaptive Y-axis tightens to trajectory range rather than CI ceiling.

---

**DEMO-138: Three scenario curves (Options A/B/C) visually indistinguishable in Zone 1A in Frames D/E**

Severity: HIGH
Confirmed by: PO Agent, Customer Agent, Development Economist, QA Lead, Frontend Architect, UX Design Thinking Agent, Chief Methodologist (7 agents)
Frames: D, E
What is shown: Three near-flat colored lines clustered near Y=0.37. Endpoint color markers (blue, orange, green) visible at step 8 but trajectory arcs from steps 0–8 appear coincident. No trajectory divergence between Option A and Option C is readable.
What should be shown: "All three scenario curves visible — distinguishable by color or label. The trajectory divergence between Option A and Option C visible — this is the visual counterpart to the 340,000 number." Zone 1B human cost finding and Zone 1A visual arc should be readable as a pair.
Root cause: (a) DEMO-137 y-axis scaling drives trajectories to bottom 10% of chart; (b) scenarios may genuinely produce similar composite scores with divergence concentrated in poverty headcount sub-indicator not visible in the composite Zone 1A view.
Fix: Resolve DEMO-137 first. After y-axis fix, if trajectories still converge: (a) add direct curve labels at endpoint for Options A/B/C; (b) if composite divergence is genuinely minimal, switch Zone 1A to poverty headcount rate for Act 2 frames where divergence is visible.

---

**DEMO-139: Choropleth centered on North America in Frames D/E; ZMB not visible; map dominates ~40% of viewport**

Severity: HIGH
Confirmed by: Customer Agent, QA Lead, UX Design Thinking Agent, UX Designer (4 agents)
Frames: D, E
What is shown: Lower ~40% of viewport in Frames D/E shows a full-color choropleth rendered at an Arctic/North Atlantic centered view. Zambia (sub-Saharan Africa) is outside the visible map boundary. The large colored map draws the eye before Zone 1B's comparison summary.
What should be shown: UX-RULING-4 compliance: "ZMB visible and highlighted — geographic context only." Choropleth should auto-pan to sub-Saharan Africa when entity switches to ZMB.
Fix: (1) Trigger `centerOnEntity('ZMB')` after scenario navigation in `demo-narrated.spec.ts` before Frame D/E captures. (2) Verify choropleth pan/zoom state responds to entity change when loaded via comparison URL. For screenshots, consider cropping viewport above the choropleth separator to exclude it from captures D/E entirely — choropleth has no role in the Act 2 analytical argument.

---

**DEMO-140: Zone 1B temporal contradiction — "No active threshold breaches" header alongside CRITICAL badge alerts in the same panel (Frames A/B/C)**

Severity: HIGH
Confirmed by: UX Designer (HIGH), Customer Agent, Frontend Architect (HIGH), UX Design Thinking Agent (HIGH) (4 agents)
Frames: A, B, C
What is shown: Zone 1B top section: "No active threshold breaches. No other active alerts." (MDA framework-level). Below: COHORT IMPACT section with two CRITICAL (red badge) rows: "Bottom Quintile Agricultural Workers" and "Bottom Quintile Formal Sector" — sub-text "Threshold crossed at step 1 · 3.75% below floor."
Issue: The MDA alert section and COHORT IMPACT section operate on different temporal scopes — current step vs. historical breach at step 1. A first-time viewer reads "no breaches" then sees CRITICAL red — cannot determine current safety state. The Mode 3 cognitive task is "threshold-safe path construction"; Zone 1B's contradictory signals undermine that task.
Fix: Distinguish temporal scope in Zone 1B: "No MDA breaches at step [N]" with a step label for current-step MDA state. Historical cohort breaches in COHORT IMPACT: use amber "Breached at step 1" badge rather than CRITICAL red, reserving CRITICAL red for current-step violations requiring action now.

---

**DEMO-141: TERMINAL Reserve Coverage financial alert occupies visual headline position above DistributionalComparisonSummary in Frames D/E**

Severity: HIGH
Confirmed by: Development Economist, PO Agent, QA Lead, UX Designer (4 agents)
Frames: D, E
What is shown: Zone 1B vertical order: (1) Option A/B/C crossing summary rows; (2) TERMINAL dark-red block "Reserve Coverage (months) FIN ZMB — Step 8" — most visually dominant element; (3) DistributionalComparisonSummary (truncated, below fold).
Issue: In Act 2, the human cost finding (+342,737 persons) is the primary conclusion — the reserve coverage financial alert is supporting context. A first-time viewer's eye goes to financial crisis (TERMINAL red) then to human cost (truncated, below). Human cost parity standard inverted.
Fix: Reorder Zone 1B for comparison sessions: DistributionalComparisonSummary first (sticky), TERMINAL alert below. Alternatively, visually distinguish comparison summary with border or larger type that draws the eye above the alert block.

---

**DEMO-142: Internal jargon "Policy Malevolent Margin — historical" exposed in DistributionalComparisonSummary in Frames D/E**

Severity: HIGH
Confirmed by: Data Architect (HIGH), Customer Agent (noted)
Frames: D, E
What is shown: DistributionalComparisonSummary partial row reads "Policy Malevolent Margin — historical: +342,727 persons" or similar.
Issue: "Policy Malevolent Margin" is an internal tool term, not a stakeholder-facing label. A finance minister or IMF negotiating team member who reads this label will be distracted by the unexplained jargon and may question whether the tool is adversarially framed.
Fix: Replace with a stakeholder-appropriate label. Candidates: "Poverty impact differential — observed trend", "Option A vs. Option C: poverty headcount differential", or simply "Poverty headcount differential" (already shown as the section subheading). Fix in `DistributionalComparisonSummary.tsx` display layer before any demo presentation.

---

**DEMO-143: Human Cost Ledger bottom quintile indicator unpopulated (em dash value) across all five frames and both scenarios**

Severity: HIGH
Confirmed by: Development Economist (CRITICAL ×2 findings), Customer Agent
Frames: A, B, C, D, E (all)
What is shown: Human Cost Ledger in all five frames shows "Bottom Quintile ... —" — the value field displays an em dash for both SEN (steps 3 and 6) and ZMB (terminal step). Other Human Cost Ledger rows (Poverty %, Unemployment %) show populated values.
Issue: The Human Cost Ledger is a primary output surface specified by the "human cost parity" principle (equal visual weight alongside financial indicators). The bottom quintile informal workers indicator is the Act 1 primary measurement subject. Its consistent absence across five frames and two scenarios indicates a data loading failure — indicator key mismatch or fixture not populating the field.
Fix: Verify the indicator key in the scenario fixture matches the key expected by the Human Cost Ledger component. Run `GET /api/v1/scenarios/{id}/cohort-indicators` for both SEN and ZMB scenarios and confirm the bottom quintile informal workers headcount appears as non-null. Fix key registration before recapture.

---

**DEMO-144: Walkthrough narration scripts "+340,000 persons" but simulation output shows ~342,737**

Severity: HIGH
Confirmed by: Chief Methodologist (MEDIUM-HIGH), Data Architect (noted), Customer Agent (noted)
Frames: D (narration)
What is shown: Walkthrough Section 2 Step 4 scripts presenter: "plus 340,000 persons below poverty threshold. 295,000 to 395,000." Frame D partial number visible: "+342,727 persons" (or "+342,737" per Chief Methodologist's reading).
Issue: The demo's central claim is "it is a specific number." A presenter scripted to say "340,000" while the screen displays "342,727" introduces a visible discrepancy at the exact moment the demo is asserting numerical precision. The screenshot brief (Note 4) explicitly flagged this risk and instructed to update the walkthrough — but the walkthrough was not updated.
Fix: Update walkthrough Section 2 Step 4 narration to the actual simulation output value (verify against the final captured screenshot). Add a presenter note: "Read the exact figure from the screen — do not use the briefed number, as simulation outputs may vary slightly between runs." Update the Screenshot Reference table caption.

---

**DEMO-145: CI band visual prominence inverted — Mode 3 bands (5% opacity) appear more prominent than Mode 1/2 bands (12% opacity)**

Severity: HIGH
Confirmed by: Chief Methodologist (MEDIUM), QA Lead (HIGH as secondary effect)
Frames: A/B (Mode 3) vs D/E (Mode 1/2)
What is shown: In Mode 3 frames (A/B/C), Zone 1A CI bands are clearly visible as a large blue fill occupying most of the chart. In Mode 1/2 frames (D/E), Zone 1A three-scenario trajectories appear with narrow or near-invisible CI bands.
Issue: The screenshot brief states "Frame B and Frames D/E have visually wider bands than Frame A." The walkthrough implies Mode 1/2 CI bands at 12% opacity should be more visible than Mode 3 bands at 5%. The observed result is the opposite. The presentation implies that uncertainty quantification disappears in Act 2 — which undercuts the "distributed CI applies to all scenarios" claim.
Root cause: SEN scenario has large T3 half-width bands producing large area fill even at 5%. ZMB "pre-cal" scenarios may have narrower bands or different tier assignment. DEMO-137 geometry bug compounds this.
Fix: Resolve DEMO-137 first. Verify ZMB scenarios receive T3 tier multiplier (1.5×) in BandingEngine. If still inverted after geometry fix, confirm `CI_BAND_OPACITY_MODE12 = 0.12` is applied to Frames D/E; consider adding explicit CI width-floor so Mode 1/2 bands are always more visible than Mode 3 for the same trajectory.

---

### MEDIUM

---

**DEMO-146: Walkthrough Screenshot Reference table filenames don't match actual files in `docs/demo/m18/screenshots/`**

Severity: MEDIUM
Confirmed by: Customer Agent, Chief Methodologist, Data Architect
What is shown: Walkthrough reference table lists: `frame-a-active-control.png`, `frame-b-ci-bands-psp-driver.png`, `frame-d-comparison-summary.png`, `frame-e-auditability-panel.png`. Actual filenames on disk: `frame-a-the-instrument.png`, `frame-b-uncertainty-envelope.png`, `frame-d-counter-proposal.png`, `frame-e-analytical-defence.png`. Frame C matches in both.
Fix: Update walkthrough Screenshot Reference table to match disk filenames. The spec file (`demo-narrated.spec.ts`) is the canonical filename authority; the walkthrough table should derive from it.

---

**DEMO-147: T3→T4 confidence tier degradation between step 3 and step 6 unacknowledged in narration**

Severity: MEDIUM
Confirmed by: Chief Methodologist (HIGH), QA Lead (MEDIUM)
Frames: A (step 3, T3 badges) vs C (step 6, T4 badges)
What is shown: COHORT IMPACT rows in Frame A (step 3) show T3·Inferred badges. Same rows in Frame C (step 6) show T4·Model estimate badges. Human Cost Ledger follows the same pattern. The walkthrough narration asserts T3 throughout Act 1 without acknowledging the tier change.
Issue: A domain economist in the audience will notice the badge change and ask why it degrades mid-demo. If the degradation is intentional (step-depth-driven output tier decay), it must be disclosed. If it is a bug, it must be fixed.
Fix: Determine whether T4 at step 6 is intentional (simulation engine step-depth tier transition rule) or a bug. If intentional: add one sentence to Frame C narration acknowledging the T3→T4 transition. Add a bullet to Section 6 Honest Disclosures. If a bug: fix tier propagation before recapture.

---

**DEMO-148: Eight section transition statements missing from walkthrough narration per NARRATION-RULING-1**

Severity: MEDIUM
Confirmed by: UX Design Thinking Agent
What is shown: NARRATION-RULING-1 requires each section to carry umbrella statement, synthesis statement, and transition to next section. Eight transition gaps identified: Section 1→2; Frame A→B; Frame B→C; Frame C→D (Act 1→Act 2 act break, highest risk — mode and entity both change simultaneously); Frame D→E; Section 2→3; Section 3→4; Section 4→7.
Fix: Add one-sentence transitions at all eight gaps. Priority: the Act 1→Act 2 act break (Frame C→D) — "That is Act 1. Act 2 now shows what the ministry does with that finding at the negotiating table. We are switching from Senegal to Zambia, from active control to three-scenario comparison." Pre-empts stakeholder confusion at the mode/entity change.

---

**DEMO-149: Zone 1D PSP section clipped below Zone 1D bottom boundary in Frames D/E**

Severity: MEDIUM
Confirmed by: Frontend Architect
Frames: D, E
What is shown: Zone 1D renders Financial, HD, Ecological, Governance values, governance horizon note text, then "POLITICAL RISK" label at the very bottom edge — the PSP survival probability and `psp-driver-row` are below the visible Zone 1D boundary. (Distinct from DEMO-132 where PSP section IS visible but driver row is absent — here the entire PSP section overflows.)
Root cause: Governance value (0.22 for ZMB) triggers more explanatory text in Zone 1D, consuming vertical space that pushes PSP below the fold.
Fix: Reduce governance horizon note font size or collapse to tooltip disclosure in Act 2 frames. Alternatively ensure Zone 1D has sufficient minimum height to accommodate all four framework values + PSP section + driver row regardless of governance note verbosity.

---

**DEMO-150: Ecological framework value missing (em dash) in Zone 1D across all frames**

Severity: MEDIUM
Confirmed by: QA Lead (MEDIUM), Data Architect
Frames: A, B, C, D, E (all)
What is shown: Zone 1D renders "Ecological / 1.0 = boundary / [T3 · pre-cal]" with dashes (—) for the numeric value in all frames. Frames D/E show "approaching resource floor — freshwater · pre-cal" note without a corresponding numeric value.
Issue: The "four-framework current position" (UX commitment #1) requires all four values to display. A missing Ecological value weakens the "full instrument cluster" claim. The "approaching resource floor" note is also contradictory — if a threshold approach is detected, a value should exist.
Fix: Check whether SEN and ZMB demo fixtures include Ecological module output. If module not producing output, replace dashes with explicit "No data" display and add disclosure note. If rendering bug, fix Ecological value binding in `FourFrameworkZone1D.tsx`.

---

**DEMO-151: Human Development trajectory absent from Zone 1A — only Financial composite shown in the primary chart**

Severity: MEDIUM
Confirmed by: Development Economist (HIGH)
Frames: A, B, C
What is shown: Zone 1A chart displays Financial composite trajectory (baseline/branch) only. Zone 1D shows HD value (0.46, orange) as a scalar but no HD trajectory arc appears in Zone 1A.
Issue: Zone 1A — the largest visual element in the instrument cluster — communicates only Financial trajectory over time. Human Development trajectory has no temporal arc visible. Given that Act 1's primary question is about a human cost threshold (bottom quintile informal workers poverty headcount), the HD composite should be the default Zone 1A display in Mode 3 Act 1, not Financial.
Fix: Default Zone 1A to HD composite trajectory when Act 1 demo enters Mode 3 with a cohort threshold monitored. Alternatively, render HD trajectory as a second curve in Zone 1A alongside Financial, encoded in the orange color consistent with Zone 1D.

---

### LOW

---

**DEMO-152: Scenario breadcrumb in Frames D/E shows only "OptionC" — three-scenario comparison state not evident from header**

Severity: LOW
Confirmed by: UX Design Thinking Agent
Frames: D, E
What is shown: Scenario header reads "Scenario: ZMB Demo 7 OptionC 2026-06-29-q1r614 / Entity: ZMB / Status: Complete (8 steps)." Only Option C is named; three-scenario comparison mode not indicated in header.
Fix: Extend breadcrumb to indicate comparison mode — e.g., "ZMB · 3 scenarios · Reference: OptionC". If breadcrumb format is fixed, add presenter note to proactively call out that all three are loaded.

---

**DEMO-153: Act 1 narration expresses the 0.40 floor finding as an index value with no translation to people count**

Severity: LOW
Confirmed by: Development Economist
Narration: walkthrough Section 2 Step 3
What is shown: Act 1 conclusion narration says "The recovery floor is 0.40. The threshold has not been crossed." No people-count translation provided.
Issue: Act 2 correctly uses people language throughout ("Three hundred and forty thousand additional Zambians"). Act 1 uses index language. A stakeholder hearing "the 0.40 floor has not been crossed" understands a technical benchmark but not a human consequence.
Fix: Add one sentence after the floor statement: "That threshold represents [N persons] who would fall below the poverty line if the floor were crossed — under this configuration, they do not." Consistent with Act 2 people-language standard.

---

## PM Agent consolidated findings table

| ID | Severity | Frames | Agent consensus | Issue |
|---|---|---|---|---|
| DEMO-130 | CRITICAL | A, B | 9/9 | Frame B = Frame A (duplicate capture) |
| DEMO-131 | CRITICAL | E | 8/9 | Zone 3 panel content not in viewport |
| DEMO-132 | CRITICAL | All | 9/9 | `psp-driver-row` absent — G2 not visible |
| DEMO-133 | CRITICAL | D, E | 9/9 | DistributionalComparisonSummary full sentence below fold |
| DEMO-134 | CRITICAL | C | 7/9 | Wrong cohort — informal workers row absent from Zone 1B |
| DEMO-135 | CRITICAL | A | 4/9 | Frame A at step 3 not step 8 (Step 5d: max divergence at step 8) |
| DEMO-136 | CRITICAL | C | 2/9 | Frame C branch delta = 0.00 at Act 1 conclusion step |
| DEMO-137 | HIGH | All | 4/9 | CI band geometry: fills to chart ceiling; y-axis scales to band |
| DEMO-138 | HIGH | D, E | 7/9 | Three scenario curves indistinguishable in Zone 1A |
| DEMO-139 | HIGH | D, E | 4/9 | Choropleth centered on North America; ZMB off-screen; ~40% viewport |
| DEMO-140 | HIGH | A, B, C | 4/9 | Zone 1B temporal contradiction: "no breaches" + CRITICAL badges |
| DEMO-141 | HIGH | D, E | 4/9 | TERMINAL alert headline-above comparison summary — hierarchy inverted |
| DEMO-142 | HIGH | D, E | 2/9 | "Policy Malevolent Margin" internal jargon exposed in UI |
| DEMO-143 | HIGH | All | 2/9 | Human Cost Ledger bottom quintile em dash — not populated |
| DEMO-144 | HIGH | D | 3/9 | Walkthrough scripts 340,000; simulation shows ~342,737 |
| DEMO-145 | HIGH | A–E | 2/9 | CI band prominence inverted: Mode 3 bands > Mode 1/2 bands visually |
| DEMO-146 | MEDIUM | — | 3/9 | Walkthrough filename table doesn't match actual screenshot files |
| DEMO-147 | MEDIUM | A vs C | 2/9 | T3→T4 tier degradation step 3→6 unscripted |
| DEMO-148 | MEDIUM | Narration | 1/9 | Eight NARRATION-RULING-1 transitions missing |
| DEMO-149 | MEDIUM | D, E | 1/9 | Zone 1D PSP section clipped below Zone 1D boundary |
| DEMO-150 | MEDIUM | All | 2/9 | Ecological framework value missing (em dash) in Zone 1D |
| DEMO-151 | MEDIUM | A, B, C | 1/9 | HD trajectory absent from Zone 1A — Financial composite only |
| DEMO-152 | LOW | D, E | 1/9 | Breadcrumb shows only "OptionC" — comparison mode not evident |
| DEMO-153 | LOW | Narration | 1/9 | Act 1 narration missing people-count translation for 0.40 floor |

**Total: 7 CRITICAL · 9 HIGH · 6 MEDIUM · 2 LOW · 24 findings**

---

## Step 6b gate verdict

**FAIL — unanimous. No frame passes without CRITICAL findings.**

All five frames fail. Frames A and B are identical screenshots (DEMO-130). Frames D and E differ by 4 bytes — a toggle icon, not a rendered panel (DEMO-131). Frame C shows the wrong cohort (DEMO-134) with zero policy divergence at the conclusion step (DEMO-136). Frame D shows the central claim (+342,737 persons) truncated without its CI qualifiers (DEMO-133). Frame E shows the G5 analytical defence capability inaccessible in the viewport (DEMO-131).

**Minimum resolution path before recapture and Step 6b re-review:**

1. Fix `psp-driver-row` rendering in `FourFrameworkZone1D.tsx` — confirm `data-testid="psp-driver-row"` visible at 1440×900 (DEMO-132)
2. Fix CI band fill geometry in `TrajectoryView.tsx` — fill should be `[mean - halfWidth, mean + halfWidth]`, not floor-to-trajectory or trajectory-to-ceiling (DEMO-137)
3. Fix `DistributionalComparisonSummary` sticky-bottom and width constraint so all six content elements visible without scroll at 1440×900 (DEMO-133)
4. Fix Zone 3 expanded panel content to render within Zone 1B viewport height budget (DEMO-131)
5. Verify and fix bottom quintile informal workers cohort row in Zone 1B for SEN fixture at step 6 — or add "monitored" row state to `CohortImpactSection` that displays threshold-cleared indicators (DEMO-134)
6. Fix Human Cost Ledger bottom quintile indicator data pipeline — key mismatch between fixture and component (DEMO-143)
7. Re-capture Frame A at step 8/8 (DEMO-135); Frame B at step 3/8 (will be naturally distinct after DEMO-135); confirm Frame C at step 8/8 or maximum-divergence step with visible branch delta (DEMO-136)
8. Fix choropleth to center on ZMB for Act 2 frames (DEMO-139)
9. Replace "Policy Malevolent Margin" with stakeholder-appropriate label (DEMO-142)
10. Update walkthrough narration value to match actual simulation output (DEMO-144) and update Screenshot Reference table filenames (DEMO-146)

**Step 7 (Independent Review Agent) gate is blocked until Step 6b re-review returns PASS.**

---

## GitHub issues filed

| DEMO-NNN | Severity | GitHub issue |
|---|---|---|
| DEMO-130 | CRITICAL | #1459 |
| DEMO-131 | CRITICAL | #1460 |
| DEMO-132 | CRITICAL | #1461 |
| DEMO-133 | CRITICAL | #1462 |
| DEMO-134 | CRITICAL | #1463 |
| DEMO-135 | CRITICAL | #1464 |
| DEMO-136 | CRITICAL | #1465 |
| DEMO-137 | HIGH | #1466 |
| DEMO-138 | HIGH | #1467 |
| DEMO-139 | HIGH | #1468 |
| DEMO-140 | HIGH | #1469 |
| DEMO-141 | HIGH | #1470 |
| DEMO-142 | HIGH | #1471 |
| DEMO-143 | HIGH | #1472 |
| DEMO-144 | HIGH | #1473 |
| DEMO-145 | HIGH | #1474 |

**Sprint journal issue:** #1475 (M18 G6 — Demo 7 Preparation and Live Stakeholder Demo)

**DEMO-NNN namespace: last assigned DEMO-153 (next available DEMO-154)**

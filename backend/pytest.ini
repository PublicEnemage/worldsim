[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests requiring a live database (skip with -m "not integration")
    backtesting: marks backtesting fidelity tests requiring a live database with seeded entities (skip with -m "not backtesting")
